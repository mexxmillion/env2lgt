# docs/

This folder holds documentation assets referenced from the top-level
`README.md`.

- `preview.png` — main-window screenshot used as the README hero image.
  Capture from the running app with a representative HDRI loaded and a
  couple of light quads placed.
